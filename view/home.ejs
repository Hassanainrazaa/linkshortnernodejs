<!DOCTYPE html>
<html lang="en">

<head>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f7f9fb;
            margin: 0;
            padding: 20px;
            /* Added padding to ensure content isn't too close to the edges */
        }

        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            width: 100%;
            margin: 0 auto;
            /* Centering the container horizontally */
        }

        h1 {
            text-align: center;
            color: #2c3e50;
            margin-bottom: 20px;
            font-size: 2.5rem;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-bottom: 40px;
        }

        label {
            font-weight: 500;
            color: #34495e;
            font-size: 1.2rem;
        }

        input[type="text"] {
            padding: 12px;
            font-size: 1.1rem;
            border: 2px solid #bdc3c7;
            border-radius: 5px;
            transition: border 0.3s;
        }

        input[type="text"]:focus {
            outline: none;
            border-color: #2980b9;
        }

        button {
            padding: 12px;
            font-size: 1.1rem;
            color: #fff;
            background-color: #2980b9;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #3498db;
        }

        .url-generated {
            text-align: center;
            background-color: #e1f5fe;
            color: #007bb5;
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
        }

        .url-generated a {
            color: #2980b9;
            text-decoration: none;
            font-weight: bold;
        }

        .table-container {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        th,
        td {
            text-align: left;
            padding: 12px 15px;
            border-bottom: 1px solid #ecf0f1;
        }

        th {
            background-color: #3498db;
            color: white;
            font-size: 1.1rem;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        td {
            color: #34495e;
        }

        .no-urls {
            text-align: center;
            font-size: 1.2rem;
            color: #7f8c8d;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }

            table,
            th,
            td {
                font-size: 0.9rem;
            }
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
</head>

<body>
    <div class="container">
        <h1>URL Shortener</h1>

        <% if (locals.id) { %>
            <div class="url-generated">
                <p>URL Generated: <a href="http://localhost:8000/url/<%= id %>">http://localhost:8000/url/<%= id %></a>
                </p>
            </div>
            <% } %>

                <div>
                    <form method="POST" action="/url">
                        <label for="longURL">Enter a long URL:</label>
                        <input type="text" id="longURL" name="url" required placeholder="www.example.com">
                        <button type="submit">Shorten URL</button>
                    </form>
                </div>

                <div class="table-container">
                    <% if (locals.urls && urls.length> 0) { %>
                        <table>
                            <thead>
                                <tr>
                                    <th>S. no</th>
                                    <th>Short ID</th>
                                    <th>Redirect</th>
                                    <th>Clicks</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% urls.forEach((url, index)=> { %>
                                    <tr>
                                        <td>
                                            <%= index + 1 %>
                                        </td>
                                        <td>
                                            <%= url.shortId %>
                                        </td>
                                        <td><a href="<%= url.redirectURL %>" target="_blank">
                                                <%= url.redirectURL %>
                                            </a></td>
                                        <td>
                                            <%= url.visitHistory.length %>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                        <% }%>

                </div>
    </div>
</body>

</html>